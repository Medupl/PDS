\chapter{Projeto do filtro}
\label{cap:metodologia}

Dados fornecidos pelo enunciado, reunidos na Tabela~\ref{tab:dados}.

\begin{table}[htb]
\centering
\caption{Dados fornecidos pelo enunciado.}
\label{tab:dados}
\begin{tabular}{ll}
\hline
\textbf{Parâmetro} & \textbf{Valor} \\
\hline
Taxa de amostragem $F_s$ & $1{,}92$~MHz \\
Fator de decimação & $4$ (nova taxa: $480$~kHz) \\
Passagem & até $100$~kHz \\
Rejeição & a partir de $200$~kHz (canal adjacente) \\
Ondulação na passagem & $0{,}1$~dB \\
Atenuação na rejeição & $60$~dB \\
\hline
\end{tabular}
\fonte{Autoria própria.}
\end{table}

Todas as verificações são feitas sobre o mesmo sinal de teste, gerado pelo código da Figura~\ref{fig:codigo-sinal}. Ele soma quatro parcelas: o canal útil em $30$~kHz com amplitude $1$ e uma modulação lenta de amplitude, o canal adjacente em $200$~kHz com amplitude $8$, o interferente em $450$~kHz com amplitude $6$ e um ruído complexo de baixo nível. A semente do gerador é fixada para que o sinal seja o mesmo em todas as execuções.

\begin{figure}[htb]
\centering
\caption{Geração do sinal de teste.}
\label{fig:codigo-sinal}
\begin{minipage}{0.9\linewidth}
\begin{verbatim}
import numpy as np

Fs = 1920000
D  = 4
np.random.seed(9)
t = np.arange(0, 4e-3, 1/Fs)
util  = np.exp(1j*2*np.pi*30000*t)
util *= (1 + 0.3*np.sin(2*np.pi*5000*t))
adj   = 8.0*np.exp(1j*2*np.pi*200000*t)
alias = 6.0*np.exp(1j*2*np.pi*450000*t)
ruido = 0.01*(np.random.randn(len(t)) + 1j*np.random.randn(len(t)))
x = util + adj + alias + ruido
\end{verbatim}
\end{minipage}
\fonte{Dados do projeto.}
\end{figure}

A Figura~\ref{fig:sinal} mostra esse sinal no tempo e no espectro. No tempo, as partes real (I) e imaginária (Q) dão uma forma de onda embaralhada, com picos passando de $15$, e o canal útil é invisível no meio dela, porque tem amplitude $1$ contra vizinhos de amplitude $8$ e $6$. No espectro, tomando o canal útil como referência de $0$~dB, cada componente aparece separada na sua frequência: o canal adjacente em $200$~kHz chega $18{,}06$~dB acima do útil, e o interferente em $450$~kHz chega $15{,}56$~dB acima.

\begin{figure}[htb]
\centering
\caption{Sinal recebido em banda base: no tempo (acima) e no espectro (abaixo), com $0$~dB tomado no canal útil.}
\includegraphics[width=\linewidth]{Imagens/fig01_sinal_recebido.png}
\label{fig:sinal}
\fonte{Autoria própria.}
\end{figure}

Esses dois números são o que o projeto precisa vencer. O filtro não tem apenas que atenuar, tem que atenuar o suficiente para derrubar interferentes que chegam mais fortes que o próprio sinal de interesse. É daí que sai a atenuação de projeto de $75{,}56$~dB, tratada na Seção~\ref{sec:atenuacao}.

\section{Especificações}

A faixa de passagem vai até $100$~kHz simplesmente porque é essa a banda que o canal útil ocupa ($\pm 100$~kHz em torno de zero). A faixa de rejeição começa em $200$~kHz porque é ali que está o canal vizinho, o primeiro sinal que não queremos deixar passar. Entre um e outro sobra uma faixa de transição de $100$~kHz, que é o espaço que o filtro tem para sair do ganho $1$ e chegar perto de zero.

Como o projeto é feito em tempo discreto, as frequências precisam ser normalizadas em relação à taxa de amostragem. Usando $\omega = 2\pi f / F_s$:

\begin{equation}
\omega_p = 2\pi\frac{100}{1920} = 0{,}3272 \ \mathrm{rad/amostra}, \qquad
\omega_s = 2\pi\frac{200}{1920} = 0{,}6545 \ \mathrm{rad/amostra},
\end{equation}

\begin{equation}
\Delta\omega = \omega_s - \omega_p = 0{,}3273 \ \mathrm{rad/amostra}.
\end{equation}

As tolerâncias de amplitude saem das duas exigências em decibéis. Na passagem, o ganho pode variar $0{,}1$~dB de pico a pico, o que dá

\begin{equation}
20\log_{10}\frac{1+\delta_1}{1-\delta_1} = 0{,}1 \ \mathrm{dB}
\quad \Longrightarrow \quad
\delta_1 = 0{,}0058 ,
\end{equation}

e na rejeição a atenuação de $60$~dB corresponde a

\begin{equation}
\delta_2 = 10^{-60/20} = 0{,}001 .
\end{equation}

\section{Escolha do tipo de filtro e do método de projeto}

Quanto à fase e ao atraso de grupo, o classificador de modulação decide sobre a constelação I/Q, onde a informação está na amplitude e na fase. Um IIR tem fase não linear, o atraso de grupo varia com a frequência e a constelação sai distorcida. Um FIR de coeficientes simétricos garante fase exatamente linear e atraso de grupo constante, e o sinal inteiro é apenas atrasado, sem deformação. Por isso o tipo escolhido foi o FIR passa-baixas de fase linear.

Dentro dos FIR de fase linear, a escolha foi $k = 0$ com $M$ ímpar, o que corresponde ao Tipo I. É a combinação que dá atraso de grupo constante e inteiro em amostras e não impõe restrição de ganho em nenhuma frequência, que é justamente o que um passa-baixas precisa.

Quanto ao método de projeto, entre as janelas vistas na disciplina a Kaiser é a única que ajusta o parâmetro $\beta$ a partir da atenuação exigida, entregando o menor comprimento possível dentro do método. A comparação com as janelas de atenuação fixa está no Capítulo~\ref{cap:desenvolvimento}.

Sobre o custo, o FIR exige $93$ coeficientes, contra ordem $6$ de um IIR elíptico projetado para a mesma máscara. A diferença parece grande, mas o filtro vem antes de uma decimação por $4$: na forma polifásica apenas uma em cada quatro saídas é calculada, e o custo real cai por um fator próximo de $4$. Além disso, obter fase linear com um IIR exigiria um equalizador de fase adicional, que devolveria em ordem o que o IIR tinha economizado. Na prática, o custo extra do FIR é inexistente.

\section{Dimensionamento e parâmetros obtidos}

O critério pede que o interferente de $450$~kHz, depois de dobrar sobre o canal, fique $60$~dB abaixo do canal útil. Só que ele não parte do mesmo nível: com amplitude $6$ contra $1$ do canal útil, chega

\begin{equation}
20\log_{10}(6) = 15{,}56 \ \mathrm{dB}
\end{equation}

acima do sinal que queremos. Para terminar $60$~dB abaixo, o filtro não pode atenuar $60$~dB, precisa atenuar

\begin{equation}
A = 60 + 15{,}56 = 75{,}56 \ \mathrm{dB}
\label{eq:atenuacao-projeto}
\end{equation}

na região de $450$~kHz. Esse é o valor que realmente dimensiona o filtro. Projetando com os $60$~dB nominais, o filtro sai com $71$ coeficientes e o interferente termina em $-57{,}4$~dB relativos ao útil, ou seja, $2{,}6$~dB acima do limite: reprovaria no critério. Por isso a atenuação de projeto adotada foi a da Equação~\eqref{eq:atenuacao-projeto}.

Vale notar que o canal adjacente, apesar de entrar ainda mais forte ($20\log_{10}(8) = 18{,}06$~dB acima do útil), é menos exigente: ele fica fora do canal depois da decimação, então basta a rejeição de $60$~dB pedida no enunciado.

Com a atenuação de projeto fechada, o filtro pode ser dimensionado. O procedimento seguido é o dos slides da disciplina. Com $\delta = \delta_2$ e a atenuação da Equação~\eqref{eq:atenuacao-projeto}, calcula-se primeiro o parâmetro da janela, que para $A > 50$~dB vale

\begin{equation}
\beta = 0{,}1102\,(A - 8{,}7) = 7{,}3683 ,
\end{equation}

e depois o comprimento do filtro, que depende da largura da faixa de transição:

\begin{equation}
M \simeq \frac{A - 7{,}95}{2{,}285\,\Delta\omega} + 1 = 91{,}42 \quad \Longrightarrow \quad M = 93 .
\end{equation}

O valor foi arredondado para cima e forçado a ímpar, para que o atraso

\begin{equation}
\alpha = \frac{M-1}{2} = 46 \ \mathrm{amostras}
\end{equation}

seja um número inteiro de amostras (filtro de Tipo I).

A frequência de corte do filtro ideal é colocada no meio da faixa de transição,

\begin{equation}
\omega_c = \frac{\omega_p + \omega_s}{2} = 0{,}4909 \ \mathrm{rad/amostra} \qquad (f_c = 150 \ \mathrm{kHz}),
\end{equation}

e a resposta ao impulso final é o produto do passa-baixas ideal pela janela:

\begin{equation}
h(n) = \underbrace{\frac{\mathrm{sen}\big(\omega_c(n-\alpha)\big)}{\pi(n-\alpha)}}_{\text{filtro ideal}}
\cdot
\underbrace{\frac{I_0\!\left[\beta\sqrt{1 - \left(1 - \frac{2n}{M-1}\right)^2}\right]}{I_0[\beta]}}_{\text{janela de Kaiser}},
\qquad 0 \le n \le M-1 .
\label{eq:hn}
\end{equation}

A função $I_0$ é a Bessel modificada de primeira espécie e ordem zero, calculada pela série

\begin{equation}
I_0(x) = 1 + \sum_{k=1}^{\infty}\left[\frac{(x/2)^k}{k!}\right]^2 ,
\end{equation}

que converge rápido: para $\beta = 7{,}37$ os termos já caem abaixo de $10^{-11}$ por volta do vigésimo, então uma soma com poucas dezenas de termos dá precisão de máquina.

Os parâmetros obtidos estão reunidos na Tabela~\ref{tab:parametros}, e o resultado gráfico dessas contas é analisado na Seção~\ref{sec:resposta}.

\begin{table}[htb]
\centering
\caption{Parâmetros do filtro projetado.}
\label{tab:parametros}
\begin{tabular}{ll}
\hline
\textbf{Parâmetro} & \textbf{Valor} \\
\hline
Tolerância de projeto $\delta$ & $0{,}001$ \\
Atenuação exigida pelo canal adjacente & $60{,}00$~dB \\
Atenuação exigida pelo interferente & $75{,}56$~dB \\
Atenuação de projeto $A$ & $75{,}56$~dB \\
Parâmetro da janela $\beta$ & $7{,}3683$ \\
Comprimento $M$ & $93$ coeficientes \\
Atraso $\alpha$ & $46$ amostras ($23{,}96$~$\mu$s) \\
Frequência de corte $f_c$ & $150$~kHz ($\omega_c = 0{,}4909$) \\
\hline
\end{tabular}
\fonte{Autoria própria.}
\end{table}
